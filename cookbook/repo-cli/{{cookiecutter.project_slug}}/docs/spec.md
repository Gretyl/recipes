# {{cookiecutter.project_name}} spec

## Overview

A Click-based CLI group for {{cookiecutter.target_repo}}.

## Requirements

- Click CLI group registered as `{{cookiecutter.project_slug}}`
- Package structure: `{{cookiecutter.package_name}}.tui`
- Versioned releases via `make dist`
- CHANGELOG.md maintained per Keep a Changelog 1.1.0
