Write a Python function that returns the classic greeting, `Hello, world!`.

Expose the function as `hello_world()` in `{{cookiecutter.package_name}}/main.py`, and include a `main()` function that prints the greeting when the module is executed as a script.
